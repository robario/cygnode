'use strict';
// core
const childProcess = require('child_process');

module.exports = function (ref) {
    const home = childProcess.execFileSync(process.env.COMSPEC, ['/C', "sh -c 'echo -n $HOME'"], {encoding: 'utf8'});
    ref.homedir = function () {
        return home;
    };
};
